1. Install npm package `splat-transform` globally if you haven't already:

   ```bash
   npm install -g splat-transform
   ```
2. Run `install.bat` to install right click context menu for `.ply` files.
3. Right click on any `.ply` file and select "Compress with SOGS" to compress it using SOGS.